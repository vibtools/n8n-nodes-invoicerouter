# Provider Recipe Specification

Provider recipes define capability metadata, required account fields, lifecycle modes, lifecycle steps, and response mapping. Standard providers should be added through `config/providers/` and `template/providers/` before core code is changed.
