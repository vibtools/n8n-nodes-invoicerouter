# Scripts

Only cross-platform Node.js project helpers are retained:

- `clean.mjs`
- `format.mjs`
- `lint.mjs`
- `validate-project.mjs`

Build, test, type-check, package, and release orchestration is defined in `package.json` and `.github/workflows/`.
