# Xero Provider

MVP provider adapter scaffold for InvoiceRouter.

## Current status

- Configuration validation is implemented.
- Normalized payload and response mapping scaffolds are implemented.
- Live provider authentication and API transport are not implemented in this audited package.
- Use dry-run mode until provider-specific integration tests and credential handling are added.
